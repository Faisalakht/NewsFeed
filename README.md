# NewsFeed
RSSFeed form NewsApi.org

Its a news feed application which uses custom tablview cells and uses the API from NewsApi.

#Frameworks:
- SDWebImage to pull the images from the url and cache them without worrying about async data load on the tableview.
